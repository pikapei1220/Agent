# HiMES 实习面试资料（Java 后端校招）

> 适用经历：深圳市益普科技有限公司，Java 开发实习生，HiMES 半导体 MES 项目。
>
> 使用原则：下面的回答是“表达模板”，不要逐字背。优先记住每段的逻辑、关键类和你真正做过的部分。

## 一、面试前 10 分钟速记

### 1. 项目一句话

HiMES 是运行在金蝶云苍穹平台上的半导体制造执行系统，用于管理批次从投产到完工之间的工艺流转，涉及批次、设备、工站、治具、物料、质检和 WIP 等业务。

### 2. 我的工作一句话

我主要参与批次上下机和移站链路的插件维护，并负责或实现过 IPQC 巡检触发、工站 WIP 预警、晶圆库存查询、治具状态处理等功能，工作重点是业务校验、状态更新、数据查询和异常反馈。

### 3. 必须讲清楚的分层

```text
用户操作
  → 表单插件：页面交互、字段联动、F7 过滤、操作前确认
  → 操作插件：事务入口、注册校验器、调用业务服务、返回结果
  → Service：执行业务规则、状态变更、关联数据更新、日志记录
  → 苍穹数据服务：通过 DynamicObject、QFilter 查询和保存实体
```

### 4. 三个主案例

1. **批次上下机/移站**：先校验，再更新；批次、设备、治具、物料和日志要保持一致。
2. **IPQC 触发**：按设备累计批数、片数或颗数，达到巡检方案阈值后生成任务，并保留取模后的余量。
3. **WIP 预警**：按工站和流转单位统计等待量、作业量和总量，再与已审核启用的阈值配置比较。

### 5. 五个高频追问

- 为什么把逻辑放在 Service，而不是全部写在表单插件中？
- 上机、下机和移站分别改变哪些状态？
- 如何避免多个实体只更新一部分？
- DynamicObject 和 QFilter 是什么？与普通 MyBatis 开发有什么差别？
- 如果两个人同时操作同一个批次，怎么防止状态覆盖？

### 6. 表达边界

- **可以说负责/实现**：Git 提交能直接证明且你能讲清楚的 IPQC 触发方式、WIP 预警、晶圆库存查询、部分治具和设备异常功能。
- **建议说参与**：批次上机、下机、移站整条核心链路。它是大型团队链路，不要说成自己独立设计了整个流程。
- **建议说项目采用**：项目整体架构、团队公共 Service、平台通用插件体系。
- **不要编数字**：没有证据时，不说 QPS、数据量、性能提升百分比、线上事故损失或用户规模。

---

## 二、如何介绍这段实习

### 1. 30 秒版本

我在深圳市益普科技有限公司担任 Java 开发实习生，参与 HiMES 半导体制造执行系统的开发。项目基于金蝶云苍穹平台，通过 Java 表单插件、操作插件和 Service 实现批次上下机、移站、设备治具、质检和 WIP 管理。我参与了批次核心链路维护，并实现过 IPQC 巡检触发、工站 WIP 预警和晶圆库存查询等功能。这段经历让我比较系统地接触了复杂业务规则的分层、状态一致性、平台数据查询和异常处理。

### 2. 1 分钟版本

我第一段实习是在深圳市益普科技有限公司做 Java 开发，参与的是 HiMES 半导体 MES。这个系统负责生产现场的批次流转，例如一个批次到达某个工站后，需要选择设备上机；完成加工后下机或移站，同时还要联动治具、物料、质检任务和操作日志。

项目基于金蝶云苍穹低代码平台，但核心业务并不是简单配置，而是通过 Java 插件实现。表单插件负责交互和字段联动，操作插件是后台事务入口，Service 负责具体校验和状态变更，数据通过 DynamicObject、QFilter 和平台 ServiceHelper 操作。

我的工作一部分是参与上下机和移站链路的维护，另一部分是完成较独立的功能，比如按批数、片数、颗数触发 IPQC 巡检，以及按工站统计等待 WIP 和作业中 WIP。实习中我最大的收获是，后端不仅是把数据保存进去，更重要的是把状态变化、事务边界、异常提示和关联数据更新设计清楚。

### 3. 3 分钟版本

我在益普科技参与的是 HiMES 半导体 MES 项目。MES 位于生产计划和车间执行之间，系统要知道一个批次当前在哪个工站、是否已经上机、使用了哪台设备和哪些治具、消耗了什么物料、产生了多少良品和缺陷，以及是否触发质检或锁定规则。

项目采用 Java 8 和 Gradle，运行在金蝶云苍穹平台上。平台通过元数据定义业务对象，Java 代码一般不直接写 SQL 操作业务表，而是使用 DynamicObject 表示实体，通过 QFilter 构造条件，再调用 BusinessDataServiceHelper、QueryServiceHelper 和 SaveServiceHelper 查询或保存。

在代码分层上，我把它理解为三层。第一层是表单插件，负责页面事件，例如 registerListener、propertyChanged、beforeDoOperation，主要做字段联动、搜索过滤和操作确认。第二层是操作插件，它继承 AbstractOperationServicePlugIn，是保存或业务操作的后台入口，负责注册校验器、进入事务回调、调用 Service，并把成功、警告或失败信息返回表单。第三层是 Service，负责真正的业务规则，比如校验批次状态、设备可用性、当前工站是否一致，更新上下机状态，处理治具、物料、质检和操作日志。

我参与维护过批次上机、下机和移站链路。以上机为例，后台会重新查询批次，不能完全信任页面数据；然后校验批次是否生产中、是否已经上机、设备是否可用、页面工站和数据库工站是否一致、治具是否齐全。通过后才更新批次的上机状态、设备、时间和人员，并记录上下机数据和批次日志。下机和移站会进一步处理产出数量、缺陷、良率、QTime、物料解绑以及是否进入下一工站。

我还实现过 IPQC 巡检触发方式。设备每次下机后累计批数、片数和颗数，再读取当前工站已审核的巡检方案。如果某种累计值达到阈值，就生成巡检任务。任务生成后不是简单清零，而是对阈值取模保留余数，避免超过一个周期的数量丢失；同时检查是否已经存在未完成任务，避免重复生成。

另一个较完整的功能是工站 WIP 预警。我按生产中批次查询数据，根据批次是否上机拆成等待量和作业量；由于不同工站可能按片或按颗流转，统计数量时还要根据工站所属区域的流转单位选择 waferCount 或 qtyCurrent。阈值只读取已审核且启用的配置，并用 Map 按工站聚合，避免页面每一行都重复查询。

这段实习让我认识到，复杂业务系统最重要的是规则边界和一致性。现在如果重新设计，我会进一步补充显式状态机、乐观锁或幂等键，并把 Service 对平台静态工具类的依赖封装起来，提高单元测试能力。

---

## 三、项目和业务基础

### 1. MES 是什么

MES 是 Manufacturing Execution System，即制造执行系统。它承接生产计划并落到车间执行，关注“现在生产到哪一步、用了什么资源、结果如何、能否继续流转”。

在 HiMES 中，核心对象包括：

- **批次 Lot**：生产流转的核心载体。
- **工艺路线 Process**：定义批次需要经过哪些工站。
- **工站 Step**：当前加工环节。
- **设备 Machine**：批次实际加工使用的设备。
- **治具 Fixture**：加工辅助资源，需要校验状态和寿命。
- **物料批次 Material Lot**：生产过程绑定和消耗的物料。
- **质检任务**：FAI、IPQC、LQI 等质量控制任务。
- **WIP**：Work In Process，在制品数量。

### 2. 上机、下机和移站

- **上机**：批次在当前工站开始加工，绑定设备，记录上机时间、人员和作业记录。
- **下机**：结束当前设备上的加工，记录产出、缺陷和良率，处理治具、物料、质检等关联逻辑。某些工站下机后仍停留当前工站。
- **移站/下移**：批次从当前工站流转到下一工站，清理当前工站的上机上下文，并更新接收、确认或预锁等状态。

不要回答成“上机就是 status=1，下机就是 status=0”。它们是跨多个实体的业务操作，不是单字段修改。

### 3. 低代码平台与 Java 后端的关系

推荐回答：

> 苍穹平台提供业务对象元数据、表单运行时、插件生命周期和数据服务，相当于把通用 CRUD、页面框架和运行容器标准化了。我们仍然需要用 Java 编写业务规则、事务操作、状态变更、关联数据处理和异常反馈。所以它减少的是基础设施和页面样板代码，不是后端业务复杂度。

---

## 四、重点案例一：批次上下机与移站链路

### 1. 推荐讲法

> 我参与的是核心链路中的表单和操作插件维护，以及与设备、治具、质检规则有关的改动。整条链路是团队长期建设的，我不会说成由我独立完成；但我能够解释它的调用链、关键状态和一致性风险。

### 2. 调用链

以上机为例：

```text
LotCheckInPlugin
  → 页面搜索批次、选择设备、字段联动、F7 过滤
CheckInPlugin.endOperationTransaction
  → 重新按批次号查询数据库实体
  → 判断按片或按颗流转，选择对应 CheckInService
AbstractCheckInService.done/cando/checkIn
  → 业务校验
  → QTime、治具、物料等规则
  → 更新批次状态和上下机记录
  → SaveServiceHelper.save
```

下机和移站采用相似结构：`CheckOutPlugin` 或 `MovePlugin` 调用 `AbstractMoveService` 的不同实现。`MoveService` 提供通用下移行为，`CheckOutService` 在其基础上覆盖下机特有规则，是模板方法和继承复用的实际例子。

### 3. 上机前为什么要重新查询

页面上的数据可能已经过期。例如用户打开页面后，另一个用户已经把批次上机。如果后台直接相信页面传来的状态，就可能重复操作。因此操作插件通过批次号重新查询数据库实体，Service 再判断：

- 批次是否存在且处于生产中；
- 当前是否已经上机或已经下机；
- 是否被锁定；
- 生产设备是否存在、可用且与产品/工站匹配；
- 页面工站 ID 是否等于数据库当前工站 ID；
- 治具、物料、接收状态、印字确认等前置条件是否满足。

这能降低“陈旧页面数据”导致错误操作的概率，但严格的并发安全还需要数据库版本号、条件更新或平台锁机制配合。

### 4. 状态与关联数据

上机成功后，不只改变 `ewhc_is_check_in`，还会处理：

- 上机设备 `ewhc_check_in_mc`；
- 上机时间与人员；
- 当前工站上下机记录；
- 批次操作日志；
- 设备参数、首件风险或 QTime；
- 治具与物料的绑定或使用历史；
- 库位解绑等现场规则。

下机/移站还会处理数量、缺陷、良率、物料过期锁定、治具下机、下一工站、预锁和清理当前设备绑定。

### 5. 这个案例体现的后端能力

- 把页面交互、事务编排和业务规则分层。
- 后台重新校验，不信任前端状态。
- 用枚举表达业务状态，减少魔法值散落。
- 将多个实体更新放在同一业务操作中考虑。
- 将用户可理解的业务异常与系统异常区分。

### 6. 如果让你优化

可以回答：

> 第一，我会把批次状态变化整理成显式状态机，明确每个状态允许的操作。第二，在批次表增加版本号，保存时使用“id + version + 当前状态”作为条件，更新行数为 0 就提示状态已变化。第三，对同一操作增加业务幂等键，避免网络重试产生重复日志或任务。第四，把平台静态 ServiceHelper 封装成 Repository 接口，便于单元测试。

注意：这些是改进思路，不要说项目已经全部实现。

---

## 五、重点案例二：IPQC 巡检触发

### 1. STAR 表达

**背景：** 生产过程中需要按一定频率做巡检。不同巡检方案可能按加工批数、晶圆片数或颗数触发，不能靠人工记忆。

**任务：** 我参与增加巡检触发方式，使设备下机/下移后能够根据累计量自动判断是否生成 IPQC 任务。

**实现：**

1. 用 `IPQCTriggerTypeEnum` 表达时间、批数、片数、颗数等触发类型。
2. 在下机/下移完成后累计设备的批、片、颗数量。
3. 查询当前工站已审核的巡检方案，并检查工站是否启用巡检、是否配置检验区域。
4. 遍历方案，根据类型选择对应累计字段和阈值。
5. 达到阈值时统一创建任务；已有未完成任务时不重复创建。
6. 任务生成后对累计值执行 `% interval`，保留超过阈值的余数。

**结果：** 触发规则由配置驱动，能够支持不同生产单位，并减少重复生成巡检任务的风险。不要编造具体减少比例。

### 2. 为什么用枚举

触发类型不仅是一个字符串，还关联“如何判断启用”“读取哪个阈值字段”“累计哪个统计字段”。使用枚举可以集中表达有限状态，避免在多个 Service 里散落 `"lot"`、`"wafer"`、`"die"` 判断，也便于编译期检查和统一扩展。

### 3. 为什么取模而不是清零

假设阈值为 10，本次累计达到 23。如果触发后直接清零，会丢掉多出的 13；使用 `23 % 10 = 3`，代表两个完整周期已经达到，剩余 3 进入下一个周期。当前实现一次生成任务并保留余量，是否要一次补生成多个任务应由业务规则决定。

### 4. 并发风险

如果同一设备的两个批次几乎同时下机，两个线程可能都读到旧累计值并生成任务。可改进为：

- 对设备统计记录做乐观锁；
- 通过条件更新原子累加；
- 对“设备 + 方案 + 周期”建立唯一业务键；
- 任务创建和累计扣减放在同一事务中。

---

## 六、重点案例三：工站 WIP 预警

### 1. STAR 表达

**背景：** 车间需要看到每个工站积压了多少在制品，并区分等待加工和正在加工。当数量接近或超过阈值时需要预警。

**任务：** 我实现了昕感客户的工站 WIP 预警功能，包括阈值配置、WIP 汇总、报表展示和明细跳转。对应提交包含 `StepWipWarnService` 及表单、报表插件。

**实现：**

1. 查询生产中的批次，只加载工站、上下机状态、当前数量和晶圆片数等必要字段。
2. 预先加载工站对应的流转单位，并构建 `stepId → transferUnit` 的 Map。
3. 按批次的上下机状态将数量累计到 `waitingQty` 或 `onOperateQty`。
4. 按片流转时使用 `ewhc_wafer_count`，其他情况使用 `ewhc_qty_current`。
5. 一次加载所有已审核、启用的阈值配置，构建 `stepId → threshold` 的 Map，避免列表逐行查询。
6. 明细页根据用户点击的是等待、作业中还是总量，追加不同 QFilter。

### 2. 为什么使用 Map 聚合

列表中每个工站都需要 WIP 和阈值。如果每渲染一行就查一次批次和阈值，会出现典型 N+1 查询。当前做法是批量加载后在内存中按 stepId 聚合，查询次数更稳定。时间复杂度大致是 O(批次数 + 工站数 + 阈值数)，空间复杂度取决于工站数量。

### 3. 这个实现的限制

当前方式会把所有生产中批次加载到应用内存统计。数据规模较小时直观且便于适配平台实体；规模变大后可考虑：

- 用数据库聚合或平台聚合查询按工站 `GROUP BY`；
- 对工站和状态建立合适索引；
- 增量维护 WIP 汇总表；
- 对报表结果做短时缓存，并在上下机/移站后失效。

不要说项目已经做了这些优化；这是你对扩展性的思考。

---

## 七、补充案例：已入库晶圆查询 API

这个案例适合面试官问“你写过接口吗”时使用。

### 推荐回答

> 我实现过一个已入库晶圆查询接口。Web API 层 `WaferInStockListApi` 负责调用 Service、包装成功结果，并把参数或业务错误与系统错误映射为不同返回码；`WaferInStockListService` 负责查询批次、收集物料批次号，再批量加载晶圆明细并组装成批次下包含晶圆列表的返回结构。

值得讲的点：

- 批次查询使用 `QueryServiceHelper.query` 获取平铺字段。
- 先收集所有物料批次，再用一次 `QFilter.in` 批量加载晶圆，避免逐批次查询。
- 返回 DTO 区分批次层和晶圆层，避免把平台 DynamicObject 直接暴露给调用方。
- 当前代码存在固定 `LIMIT=100`，更完善的方案应支持调用方分页参数、稳定排序、总数查询和最大页大小限制。

---

## 八、面试官可能提出的问题与回答

### A. 项目与业务

#### 1. 你在这段实习中到底负责什么？

我会分两部分回答：一部分是参与批次上下机、下机和移站链路的表单及操作插件维护，主要接触设备、工站、治具、数量和状态校验；另一部分是相对独立的功能，包括 IPQC 巡检触发方式、工站 WIP 预警、晶圆库存查询，以及部分治具和设备异常功能。整条 MES 核心链路是团队共同维护的，我主要负责其中的具体需求和问题修复。

#### 2. 你认为最有价值的功能是什么？

如果突出业务完整性，我会选 WIP 预警，因为它包括阈值配置、Service 聚合、报表展示和明细钻取；如果突出一致性和规则设计，我会选 IPQC 触发，因为它涉及累计量、阈值匹配、任务去重和触发后余量处理。

#### 3. MES 和普通管理系统有什么不同？

MES 的状态变化更贴近物理生产过程，错误操作可能导致系统记录与车间实物不一致。一个“下机”操作往往同时影响批次、设备、治具、物料、数量、质检和日志，因此业务校验、一致性和可追溯性比普通单表 CRUD 更重要。

#### 4. 为什么需要批次日志？

制造过程必须可追溯。日志用于回答谁在什么时间、哪个工站、哪台设备上执行了什么操作，操作前后数量和状态如何。它既服务问题排查，也可能服务质量追溯和审计。日志应记录业务语义，而不只是“update success”。

#### 5. 上机和移站有什么本质区别？

上机是在当前工站开始作业，重点是绑定设备和记录开始信息；移站是改变批次当前工站，重点是结束当前环节、计算产出并进入下一节点。下机介于两者之间：它结束设备作业，但是否立即移到下一工站取决于工站配置。

#### 6. 为什么治具也要参与校验？

治具是生产资源，可能存在禁用、异常、寿命超限、型号不匹配或已经被占用等情况。只校验设备而不校验治具，会让系统允许一个现场无法执行或存在质量风险的操作。

#### 7. WIP 为什么区分等待和作业中？

总 WIP 只能说明积压规模，等待 WIP 反映排队压力，作业中 WIP 反映当前设备正在消化的数量。区分后才能判断瓶颈是设备能力不足、调度不合理，还是上游集中到料。

#### 8. IPQC 与 FAI 有什么区别？

FAI 是首件检验，通常关注首次投产或条件变化后的第一件是否合格；IPQC 是过程巡检，按时间、批数、片数或颗数等频率持续检查生产过程。项目中两者都可能阻断或提示业务操作，但触发时机和目的不同。

### B. 架构与分层

#### 9. 表单插件和操作插件有什么区别？

表单插件运行在页面生命周期中，负责点击、字段变化、F7 选择过滤和页面刷新。操作插件运行在后台业务操作生命周期中，是更可信的事务入口，负责校验器、调用 Service 和返回操作结果。关键业务规则必须在后台再次校验，不能只依赖表单插件。

#### 10. 为什么不能把所有逻辑都写在插件里？

插件应该负责适配平台事件。如果把查询、校验、状态更新都堆在插件里，PC、移动端、批量操作和 API 会重复实现，而且很难测试。抽到 Service 后，多个入口可以复用同一业务规则，插件只负责参数转换和结果反馈。

#### 11. 这个项目用了什么设计模式？

比较明显的是模板方法模式。`AbstractMoveService` 定义通用流程，不同子类覆盖操作类型、日志类型、数量计算和是否移站等差异；`CheckOutService` 又在 `MoveService` 基础上增加下机特有规则。还有策略思想：根据按片或按颗的流转单位选择不同 Service。枚举则集中管理有限业务状态。

#### 12. 继承是否使用过度？

继承适合流程骨架稳定、差异点明确的场景，但层次太深会让调用路径难以理解。像移动流程可以保留模板方法；治具、物料、质检等横向规则更适合组合成独立策略或处理器列表，避免基类持续膨胀。

#### 13. DynamicObject 是什么？

它是苍穹平台根据业务对象元数据提供的动态实体，字段在运行时通过标识读取，例如 `getString`、`getDynamicObject`。优点是与低代码元数据和平台服务统一；缺点是编译期类型检查较弱，字段写错或类型不匹配往往运行时才暴露，所以必须参考元数据并在 Service 边界转换成明确 DTO。

#### 14. QFilter 是什么？

QFilter 是平台的查询条件对象，类似类型化的 WHERE 条件构造器。可以用 QCP 表达 equals、in、范围和空值条件，也能通过 and/or 组合。使用时要注意关联字段路径、空集合的 `in`、查询字段数量和索引友好性。

#### 15. BusinessDataServiceHelper.load 和 QueryServiceHelper.query 怎么选？

需要完整实体、关联对象或后续保存时，适合用 `BusinessDataServiceHelper.load`；只做报表或读取少量平铺字段时，`QueryServiceHelper.query` 更轻量。原则是按用途选择，避免为了展示几个字段加载整张复杂单据。

#### 16. 为什么返回 DTO，而不是直接返回 DynamicObject？

DTO 能稳定 API 契约，隐藏平台字段和内部实体结构，避免序列化出不需要的关联对象；同时可以明确字段类型、做空值处理，并让调用方不依赖苍穹 SDK。

#### 17. 为什么枚举比字符串判断好？

枚举把合法取值和相关行为集中管理，减少魔法值、拼写错误和重复分支。例如巡检触发类型可以在枚举中定义取值和启用判断。需要注意数据库仍保存字符串值，因此要提供安全的 value-to-enum 转换，并处理未知值。

#### 18. 你怎么理解高内聚、低耦合？

以本项目为例，IPQC 触发 Service 应只负责“是否触发及触发后统计处理”，任务实体创建放到 TaskGenerateService；插件只负责在正确生命周期调用。这是高内聚。调用方依赖 Service 能力而不是知道所有实体字段，是降低耦合。

### C. 事务、一致性与并发

#### 19. 为什么操作插件适合作为事务入口？

因为它对应一次明确的业务操作，平台在操作生命周期内提供事务回调。将批次、日志、治具、物料等变更放在同一操作边界内，任何关键步骤失败都应让整个操作失败或回滚，避免部分成功。

#### 20. 如果保存批次成功，但保存日志失败怎么办？

理想情况是两者处于同一事务，日志失败抛出异常并整体回滚。如果日志是非关键审计日志，也可以采用事务消息或本地事件在提交后异步补写，但制造追溯日志通常比较关键，不能简单吞异常。

#### 21. 项目中如何返回业务异常？

Service 抛出 `MesRuntimeException` 表达用户可理解的业务失败，例如“当前批次已上机”或“设备异常”。操作插件捕获后，通过公共工具把失败状态和消息写入 operationResult 返回表单。系统异常则应记录完整堆栈，前端只返回通用错误和追踪标识，避免暴露内部信息。

#### 22. 为什么不建议 catch Exception 后只返回“操作失败”？

这样会丢失业务语义和排查线索。应该区分可预期的业务异常与不可预期的系统异常：业务异常给明确提示；系统异常记录上下文、堆栈和追踪 ID，再返回统一提示。

#### 23. 如何处理重复提交？

可以从三层处理：前端按钮提交后暂时禁用；后台重新校验当前状态；关键操作增加幂等键或唯一约束。仅靠前端防抖不可靠，因为网络重试和不同客户端仍可能重复请求。

#### 24. 如何处理两个用户同时操作同一批次？

只做“先查后改”仍有竞态。更稳妥的是增加 version 字段做乐观锁，更新条件包含旧版本和旧状态；或者使用数据库条件更新，只有符合预期状态才成功。更新行数为 0 时重新查询并提示用户数据已变化。极端场景才考虑悲观锁或分布式锁。

#### 25. IPQC 任务如何避免重复生成？

当前逻辑会先检查设备是否存在未完成任务，这是业务层去重。更严格的做法是在数据库建立唯一业务键，例如设备、方案和触发周期组合；任务创建与统计扣减放入同一事务，并使用乐观锁保护累计记录。

#### 26. 为什么检查顺序也很重要？

先做便宜且确定的校验，例如空值、状态和 ID，再做数据库查询和复杂规则，可以减少无效开销。更重要的是，产生副作用的动作必须放在全部阻断性校验之后，否则后续抛异常时容易留下脏数据或无意义任务。

#### 27. 事务是不是越大越好？

不是。事务要覆盖必须原子完成的状态变化，但不应包含长时间远程调用、文件处理或用户交互，否则会延长锁持有时间。可以把核心数据库状态放在短事务中，非关键通知通过事务后事件异步执行。

### D. 查询与性能

#### 28. 你做过什么查询优化？

WIP 预警中，我先一次加载工站流转单位和阈值配置，构建 Map，再遍历批次聚合，避免列表每一行重复查询。晶圆库存查询中，先收集所有物料批次号，再用一次 `QFilter.in` 批量加载晶圆，避免按批次 N+1 查询。

#### 29. `in` 条件是不是一定比循环查询好？

通常能减少网络往返，但集合很大时会导致 SQL 过长或超过数据库限制。应根据数据库限制分批，或者使用临时表、关联查询和分页。还要处理空集合，不能生成无意义或语义错误的 `in ()`。

#### 30. WIP 数据量很大怎么办？

短期可增加工站、状态等索引，并只查询必要字段；中期改为数据库聚合和分页明细；实时性要求不是毫秒级时，可维护汇总表或缓存。需要先通过执行计划和监控确认瓶颈，而不是直接加缓存。

#### 31. 如何设计分页接口？

请求应包含 pageNo、pageSize 和稳定排序字段；限制最大 pageSize；查询列表和总数；返回 total、pageNo、pageSize 和 items。深分页时可用基于主键或时间的游标分页。当前晶圆接口固定限制 100 条，是可改进点。

#### 32. 查询时为什么只选必要字段？

平台实体可能包含多层关联和单据体，加载整实体会增加数据库传输、对象构造和内存开销。报表或判断逻辑应显式指定字段；只有后续需要修改保存时才加载完整实体。

### E. Java 基础与工程能力

#### 33. WIP 聚合为什么用 HashMap？

需要根据 stepId 高频查找并累计，HashMap 平均 O(1) 查找，适合 `stepId → WipQtyPair`。如果需要按工站顺序输出，可在最后排序，或使用 LinkedHashMap 保持插入顺序。

#### 34. `map.computeIfAbsent` 有什么作用？

它在 key 不存在时创建默认值，避免先 containsKey 再 put 的重复代码。WIP 聚合中可用它为每个工站创建 WipQtyPair。映射函数不应包含复杂副作用，并发场景要选择 ConcurrentHashMap。

#### 35. 如何避免 DynamicObject 导致空指针？

读取关联对象前先判断是否为空；对可选字段定义默认值；查询时明确加载所需关联字段；在 Service 入口做参数和实体完整性校验。不要用一大段 try-catch NullPointerException 掩盖字段问题。

#### 36. 为什么数量不建议使用 double？

整数批数、片数、颗数用 int 或 long；良率等精确小数使用 BigDecimal。double 是二进制浮点，可能产生精度误差。BigDecimal 计算时要明确 scale 和 RoundingMode，并避免使用 `new BigDecimal(double)`。

#### 37. 模板方法和策略模式有什么区别？

模板方法通过继承固定流程骨架，让子类覆盖步骤；策略模式通过组合在运行时替换某项算法。项目的 `AbstractMoveService` 更接近模板方法；按不同规则选择校验器或数量计算器则可以用策略模式。组合通常比深继承更灵活。

#### 38. 如何给这些 Service 写单元测试？

当前 Service 直接调用静态平台 Helper，测试隔离比较困难。可以先抽象 Repository 和 Clock 接口，构造注入；用内存实现或 Mockito 模拟查询结果。状态规则还可提取成纯 Java 方法，重点测试合法状态、非法状态、边界数量、重复任务和异常回滚。

#### 39. 你遇到的最大困难是什么？

推荐回答：

> 最大困难不是 Java 语法，而是理解字段背后的制造语义和平台插件生命周期。同一个状态在页面、操作插件和数据库中可能处于不同时间点。我采用的方法是先查业务对象元数据和相邻模块的成熟实现，再画出操作前后状态变化，最后通过测试环境复现具体批次场景。这个过程让我形成了先明确业务不变量、再写代码的习惯。

#### 40. 这段实习你做得不够好的地方是什么？

> 刚开始我更关注需求能否跑通，对并发、幂等和可测试性考虑得不够系统。后来读核心链路后，我开始关注后台重新校验、事务回调、批量查询和异常边界。如果重新做，我会先写状态转换表和测试用例，再实现插件与 Service，并补充关键操作的版本控制和结构化日志。

#### 41. 你如何定位一个“页面提示失败但数据已经变化”的问题？

先确认操作插件处于哪个事务回调，以及异常是在提交前还是提交后发生；再根据批次号、用户、时间定位服务器日志和批次操作日志；检查所有 SaveServiceHelper 和其他副作用调用的先后顺序；用同一批次状态在测试环境复现；最后判断是事务边界错误、异常被吞、提交后回调失败，还是前端只刷新失败。

#### 42. 为什么你的实习时间不长，却做了不少内容？

> 项目基于成熟平台和已有公共框架，我不是从零搭建系统。我的贡献主要是基于现有插件和 Service 模式完成具体客户需求、功能扩展和缺陷修复。能较快进入状态，是因为相同业务场景有可参考的实现，但我仍需要理解字段、生命周期和现场规则后才能正确修改。

---

## 九、压力追问与安全回答

### 1. “这个核心链路是不是你独立写的？”

> 不是。核心链路是团队长期维护的，我参与了其中的表单、操作插件和规则改动。我能详细解释调用链和我修改过的部分。相对独立完成度更高的是 WIP 预警、晶圆库存查询和 IPQC 触发方式等需求。

### 2. “你们用的是低代码，那你是不是只配了页面？”

> 页面和业务对象有平台配置，但我的主要工作是 Java 插件和 Service。比如巡检触发需要累计统计、阈值匹配、任务去重和余量扣减；WIP 预警需要批量查询、分组聚合和流转单位适配，这些都是后端业务逻辑。

### 3. “你说保证了事务一致性，具体怎么保证？”

不要绝对化。建议回答：

> 代码利用平台操作插件的事务生命周期组织核心更新，并通过业务异常中断操作。我参与时重点避免在校验完成前产生副作用。但我不会说它完全解决了所有并发问题；从代码看，乐观锁和幂等唯一键仍是可以加强的方向。

### 4. “性能提升了多少？”

> 当时没有形成可靠的压测数据，所以我不报具体百分比。能确认的是把逐行查询改为批量加载和 Map 聚合，减少了查询往返。若要量化，我会在同一数据规模下统计 SQL 次数、接口耗时、P95 和内存占用。

### 5. “为什么没有用 Spring Boot、MyBatis？”

> 项目运行在金蝶苍穹平台，平台提供插件容器、业务对象元数据和数据服务，因此数据访问方式不是 MyBatis Mapper。但分层、事务、一致性、DTO、查询优化和异常处理等后端问题是共通的。我也能把这种经验迁移到 Spring Boot 项目中。

---

## 十、措辞替换表

| 容易被追问穿的说法 | 更准确的说法 |
|---|---|
| 我独立负责整个 MES 核心链路 | 我参与批次核心链路维护，负责其中具体需求和规则改动 |
| 我保证了分布式事务一致性 | 我利用平台操作事务组织核心更新，并识别了并发与幂等风险 |
| 我大幅提升了系统性能 | 我将逐条查询调整为批量加载和 Map 聚合，减少查询往返 |
| 我设计了完整状态机 | 项目使用枚举和前置校验管理状态；显式状态机是可继续优化方向 |
| 我解决了所有重复提交 | 后台会重新校验状态；幂等键和唯一约束还可进一步加强 |
| 低代码开发就是拖控件 | 平台承担元数据和运行时，复杂业务通过 Java 插件与 Service 实现 |

---

## 十一、源码复习索引

以下路径基于当前仓库。优先看方法，不需要从头背完整文件。

### 批次链路

- `app/plugin/form/lot/LotCheckInPlugin.java`：表单事件、页面搜索和操作前后处理。
- `app/plugin/operate/check/CheckInPlugin.java`：`onAddValidators`、`endOperationTransaction`。
- `app/common/service/impl/AbstractCheckInService.java`：`cando`、`done`、`checkIn`。
- `app/plugin/operate/check/CheckOutPlugin.java`：下机事务入口。
- `app/plugin/operate/check/MovePlugin.java`：移站事务入口。
- `app/common/service/impl/AbstractMoveService.java`：移动流程骨架。
- `app/common/service/impl/MoveService.java`：通用下移差异实现。
- `app/common/service/impl/CheckOutService.java`：下机特有规则。

以上文件共同位于：

```text
code/mes/ewhc-himes-mes/src/main/java/ewhc/himes/mes/
```

### 个人提交关联案例

- `company/xingan/service/StepWipWarnService.java`：WIP 聚合、阈值、明细过滤；提交 `02570ce1b`。
- `company/lixin/service/WaferInStockListService.java`：批次与晶圆批量查询及 DTO；提交 `4ce4eaa7d` / `e6c208f82`。
- `company/lixin/webapi/WaferInStockListApi.java`：API 结果和异常映射。
- `app/common/enums/IPQCTriggerTypeEnum.java`、`app/common/service/impl/IPQCTriggerService.java`：IPQC 触发类型和阈值匹配；历史提交 `731d45c2d`。当前分支若找不到 `IPQCTriggerService.java`，可执行：

```powershell
git show 731d45c2d:code/mes/ewhc-himes-mes/src/main/java/ewhc/himes/mes/app/common/service/impl/IPQCTriggerService.java
```

- `app/common/service/impl/IPQCTaskGenerateService.java`：巡检任务生成。
- `app/common/service/impl/FixtureService.java`：治具校验、解绑和历史；相关提交 `8447f2af8`、`2756385d8`。
- `app/common/utils/AuxiliaryMcHelper.java`：设备/辅设备过滤与校验；相关修复提交 `e4492794c`。

### 异常与公共工具

- `app/exceptions/MesRuntimeException.java`：业务异常。
- `app/common/utils/CommonUtils.java`：向表单返回错误或提示的公共逻辑。

---

## 十二、最后的背诵方法

不要背 42 个标准答案。按下面顺序练三遍：

1. 脱稿说完 1 分钟版本。
2. 画出“表单插件 → 操作插件 → Service → 数据服务”的调用链。
3. 每个主案例只记六个词：背景、输入、校验、状态、副作用、改进。
4. 能解释 `DynamicObject`、`QFilter`、事务、幂等、乐观锁和 N+1。
5. 最后练习承认边界：哪些是参与，哪些是自己提交，哪些只是优化思路。

真正有说服力的回答不是术语最多，而是能把一个批次在操作前后的状态和数据变化讲清楚。
