# HiMES 实习面试资料——互联网 Java 后端版

> 本文刻意弱化半导体和制造业知识，重点回答两个问题：你具体写了什么代码；这些代码能体现哪些通用 Java 后端能力。

## 一、先统一项目口径

面试时用一句话交代背景即可：

> HiMES 是一个生产流程管理系统，我主要使用 Java 在金蝶云苍穹平台上开发表单插件、后台操作插件和业务 Service。虽然业务场景来自制造业，但我实际解决的问题是通用的后端问题，包括复杂规则校验、状态流转、事务一致性、批量查询、数据聚合、异常处理和接口设计。

不要主动展开晶圆、工站、治具等行业概念。面试官听懂“批次相当于一个具有状态的业务订单，设备和工站是关联资源”就足够了。

---

## 二、对你具体工作的准确描述

### 1. 适合写在简历上的版本

- 参与核心业务流程的表单插件、后台操作插件和 Service 维护，基于 `DynamicObject + QFilter` 完成服务端状态校验、关联资源校验和业务状态更新，避免仅依赖前端数据执行关键操作。
- 实现配置驱动的巡检触发功能，按业务累计量匹配不同规则阈值，完成任务生成、未完成任务去重及触发后余量保留，并将统计、规则判断和任务生成拆分为独立 Service。
- 实现 WIP 预警及明细查询，批量加载业务数据和阈值配置，使用 `HashMap` 按业务节点聚合等待量与处理中数量，减少列表逐行查询造成的 N+1 问题。
- 开发已完成批次及明细查询 API，通过平铺查询、批量 `IN` 查询和 `LinkedHashMap` 完成一对多结果组装，使用 DTO 隔离平台动态实体与接口返回结构。
- 参与业务异常与资源状态逻辑改造，统一后台校验失败后的错误返回，协助排查页面状态与数据库状态不一致、关联字段缺失等联调问题。

### 2. 面试中“你具体做了什么”的推荐回答

> 我的工作可以分成四类。
>
> 第一类是核心流程维护。我参与了表单插件、后台操作插件和 Service 的开发。表单插件负责页面事件，操作插件是后台事务入口，Service 负责重新查询数据库、校验当前状态并更新关联数据。我主要做具体需求和规则改动，不会把团队的整条核心链路说成我独立完成。
>
> 第二类是规则触发。我实现过一个配置驱动的巡检触发功能：业务操作完成后按资源累计处理次数和数量，再读取已生效的规则配置；达到阈值后生成任务，并检查是否已有未完成任务。触发后使用取模保留超过阈值的余量，而不是直接把计数清零。
>
> 第三类是查询与聚合。我实现过 WIP 预警，先批量查询有效业务数据，再按节点 ID 用 HashMap 聚合等待量和处理中数量；阈值也一次加载成 Map，避免页面列表逐行查库。这个功能比较能体现我对 N+1、字段裁剪和内存聚合的理解。
>
> 第四类是接口开发。我实现过一个父子结构的列表接口，先查询最近的主记录，收集关联编号后再用一次 IN 查询加载全部明细，最后通过 LinkedHashMap 组装返回 DTO。这个版本仍有固定 100 条和内存双层匹配等改进空间，我能解释当时的实现和后续优化方案。

### 3. 你的个人代码证据

以下内容有明确的个人提交记录，可以重点讲：

| 功能 | 提交 | 主要代码量与内容 | 推荐措辞 |
|---|---|---|---|
| 巡检触发方式 | `731d45c2d` | 新增枚举、累计统计 Service、触发 Service，并改造任务生成和流程调用 | 我实现/负责该需求 |
| WIP 预警 | `02570ce1b` | 新增 Service、阈值表单、报表和明细页面，共 5 个文件 | 我实现/负责该功能 |
| 已完成记录查询 API | `4ce4eaa7d`、`e6c208f82` | 新增查询 Service 和 OpenAPI 接口 | 我实现该接口 |
| 资源自动解绑与寿命状态 | `8447f2af8`、`2756385d8` | 修改 Service、表单和操作插件 | 我参与并完成具体改动 |
| 设备异常报表 | `d0729641d` | 操作插件、列表跳转、Helper 和报表字段联动 | 我参与并完成具体改动 |
| 核心流程整体 | 团队长期代码 | 上机、下机、移站、日志、关联资源等 | 我参与维护，不说独立设计 |

### 4. 30 秒介绍

> 我在益普科技做 Java 开发实习，参与一个复杂业务流程系统的后端开发。项目基于金蝶云苍穹平台，但核心规则通过 Java 插件和 Service 实现。我的主要工作包括维护核心状态流转、实现配置驱动的任务触发、完成 WIP 聚合预警，以及开发父子数据查询 API。这段经历让我实际处理了服务端校验、事务边界、N+1 查询、DTO 组装、异常返回和并发一致性等后端问题。

### 5. 90 秒介绍

> 我在益普科技担任 Java 开发实习生，项目是一个业务规则比较复杂的流程管理系统。技术上使用 Java 8 和 Gradle，运行在金蝶云苍穹平台。平台提供动态实体和插件生命周期，我们通过表单插件处理页面交互，通过后台操作插件承接事务操作，再把可复用规则放到 Service 中。
>
> 我做得比较完整的第一个功能是配置驱动的任务触发。每次业务操作完成后，系统按资源累计处理次数和数量；Service 查询生效中的规则，判断哪些规则达到阈值，排除已有未完成任务后创建新任务。触发后通过取模保留余量。这个功能让我重点考虑了职责拆分、配置驱动、重复任务和事务一致性。
>
> 第二个功能是 WIP 预警。列表需要展示每个节点的等待量、处理中数量和阈值。我没有在每一行查询数据库，而是一次加载有效数据和阈值，再用 HashMap 聚合并回填；详情查询则根据用户点击的列动态组合过滤条件。
>
> 我还开发过一个父子数据列表 API，采用两阶段批量查询：先查主记录并收集关联编号，再一次加载明细，最后组装 DTO。回头看，这个接口在分页、匹配复杂度和异常信息保护方面还有优化空间。我认为这段实习最大的收获，是开始从“功能能跑”转向关注分层、数据一致性、查询次数和可测试性。

---

## 三、三个最值得讲的代码案例

## 案例一：配置驱动的任务触发

### 业务代码结构

```text
AbstractMoveService
  → McCheckoutStatsService.accumulateOnMove
      累加处理次数、数量 A、数量 B
  → IPQCTriggerService.tryTriggerOnCheckout
      加载规则 → 匹配阈值 → 检查未完成任务
      → IPQCTaskGenerateService 创建任务
      → 扣减已消费的累计值
```

### 你具体写了什么

1. 增加触发类型枚举，将时间、次数、不同数量维度集中管理。
2. 新增累计统计 Service，在目标业务操作成功后更新资源维度的累计值。
3. 新增触发判断 Service，读取已生效规则并匹配对应阈值。
4. 调用已有任务生成能力，将多个命中的规则统一放入任务。
5. 生成前检查是否存在未完成任务，降低重复生成概率。
6. 生成后使用 `% threshold` 保留余数。

### 这个案例体现什么

- 配置驱动代替硬编码规则。
- 枚举管理有限业务类型。
- 将“统计”“判断”“任务生成”拆成不同 Service。
- 识别幂等、并发和事务一致性问题。
- 能对自己的初版实现提出改进，而不是只说“功能完成”。

### 主动指出的改进点

- `hasPendingTask(mcId)` 的去重粒度可能过粗，更合理的是资源 ID、规则 ID 和业务周期组成唯一键。
- “读取累计值—判断—创建任务—扣减累计值”存在并发竞态，应增加乐观锁或条件更新。
- 同一种触发类型如果允许配置多个不同阈值，当前取第一个阈值扣减的逻辑需要重新定义。
- 多处直接 `return` 会隐藏配置缺失，应该增加有等级的结构化日志和监控指标。
- Service 直接 new 依赖、调用静态 Helper，不利于单元测试，可改成构造注入和 Repository 抽象。

## 案例二：WIP 聚合预警

### 业务代码结构

```text
StepWipReportDataPlugin
  → StepWipWarnService.buildWipQtyMap
      一次加载所有有效记录
      一次加载节点配置
      HashMap<节点ID, WipQtyPair> 聚合
  → buildThresholdMap
      一次加载所有有效阈值
      HashMap<节点ID, 阈值配置>
  → 列表回填与详情跳转
```

### 你具体写了什么

1. 查询有效业务记录，只选择聚合需要的字段。
2. 提前加载节点的统计单位，构建 ID 到单位的 Map。
3. 使用 `computeIfAbsent` 创建每个节点的聚合对象。
4. 根据业务状态累计等待量或处理中数量。
5. 一次加载已审核且启用的阈值配置并按节点建立索引。
6. 详情页根据点击列追加不同 QFilter，复用同一个查询入口。

### 这个案例体现什么

- 避免列表逐行查询导致 N+1。
- 使用 HashMap 完成 O(n) 聚合。
- 字段裁剪、空数据处理和默认配置。
- 列表汇总与明细查询职责拆分。
- 能分析内存聚合在数据量扩大后的瓶颈。

### 主动指出的改进点

- 当前会把所有有效记录加载到应用内存，规模扩大后应改为数据库聚合或增量汇总。
- 若列表访问频繁而数据变化没那么快，可增加短 TTL 缓存，并在状态变化后失效。
- 默认统计单位可能掩盖配置错误，严格模式下应该记录告警或拒绝统计。
- 内部聚合计数如果可能超过 `int`，应该改用 `long`。
- 统计口径必须写成可测试的纯函数，避免以后列表和详情出现口径不一致。

## 案例三：父子数据查询 API

### 业务代码结构

```text
WaferInStockListApi.list
  → WaferInStockListService.listInstockLots
      QueryServiceHelper.query 查询最近主记录
      收集所有关联批次号到 HashSet
      BusinessDataServiceHelper.load + QFilter.in 批量查明细
      LinkedHashMap 保留主记录顺序并组装一对多 DTO
  → CustomApiResult 返回结果
```

### 你具体写了什么

1. 编写 OpenAPI Controller，区分业务异常和系统异常。
2. 使用平铺查询读取最近 100 条主记录，并按时间倒序。
3. 用 `HashSet` 去重收集所有关联编号。
4. 用一次 `IN` 查询加载全部明细，避免每条主记录单独查明细。
5. 使用 `LinkedHashMap` 保存主记录顺序，并组装父子 DTO。
6. 处理逗号分隔字段、空值和可选明细范围。

### 主动指出的改进点

- 固定 `LIMIT=100` 不是真正分页，应支持 pageNo/pageSize 或游标分页。
- 当前主记录和全部明细存在双层遍历，最坏复杂度接近 O(n×m)；可先将明细按关联编号分组，再按主记录合并。
- `total` 当前只是本次返回条数，不是满足条件的总记录数，接口语义要明确。
- `IN` 集合过大时需要分批或改为关联查询。
- 系统异常不应把 `ex.getMessage()` 直接返回调用方，应记录堆栈并返回统一错误和 traceId。
- 日期应优先返回标准 ISO-8601 或时间戳，而不是由 Service 固定格式化字符串。

---

## 四、互联网 Java 面试高频业务代码问题

以下问题按真实追问顺序组织。回答时先说结论，再结合你的代码，最后补充改进方案。

### A. 个人工作与代码边界

### 1. 你在项目中最核心的贡献是什么？

> 我最适合重点讲三个功能：配置驱动的任务触发、WIP 聚合预警和父子数据查询 API。这三个功能有明确的个人提交记录，分别体现规则设计、批量聚合和接口开发。核心状态流转是团队代码，我参与维护和具体规则改动，不会说成独立完成整条链路。

### 2. 哪个功能是你独立完成度最高的？

> WIP 预警的完整度最高。我新增了聚合 Service、阈值配置表单、报表数据插件、报表交互和明细页。后端重点是批量查询、按节点聚合和阈值映射，页面只消费计算结果。

### 3. 你写的是低代码，和普通 Java 后端有什么关系？

> 平台替代了部分 Controller、ORM 和页面框架，但业务 Service 仍然是 Java。我要处理的仍是服务端校验、状态流转、事务、查询次数、一对多组装、异常体系和并发。`DynamicObject + QFilter` 可以类比动态实体加查询构造器，平台插件生命周期可以类比框架提供的请求和事务扩展点。

### 4. 为什么不把所有功能都说成自己负责？

> 大型业务系统的核心链路是团队长期维护的。我的价值在于能准确说明自己改了哪些 Service 和规则，并能解释整个调用链。面试中区分个人提交和团队架构，比夸大范围更可信。

### 5. 你如何快速理解一个陌生业务模块？

> 我会从操作入口向下追踪：先找表单或 API 入口，再找操作插件的事务回调，然后定位 Service、实体字段和状态枚举；接着画出输入、前置条件、状态变化和副作用。最后用一个真实测试数据走通成功和失败路径。

### 6. 你如何证明这些代码是你写的？

> 我能给出具体提交和类：巡检触发是 `731d45c2d`，WIP 预警是 `02570ce1b`，查询 API 是 `4ce4eaa7d`。更重要的是，我能解释每个方法为什么这样设计、有哪些边界和哪里需要优化。

### B. 分层与职责设计

### 7. 表单插件、操作插件和 Service 为什么要分开？

> 表单插件只处理交互和展示；操作插件承接后台业务操作和事务生命周期；Service 封装可复用规则。关键规则必须在 Service 或后台操作层再次校验，因为前端数据可能被篡改或已经过期。这样 PC、移动端、批量操作和 API 也能复用同一规则。

### 8. 你的 Service 是否真正做到了单一职责？

> 部分做到了，例如累计统计、触发判断和任务创建是分开的。但初版还有改进空间：触发 Service 同时负责加载配置、匹配、去重和扣减统计，可以继续拆成 RuleRepository、TriggerMatcher 和 TaskCreator，便于测试与替换。

### 9. 为什么 Controller/API 层不能直接操作 DynamicObject？

> 直接操作会让接口层知道大量平台字段，导致业务逻辑分散，也容易把内部对象直接序列化出去。API 层应该负责参数和协议，Service 负责业务，Repository 负责平台数据访问，最终返回稳定 DTO。

### 10. 什么时候使用继承，什么时候使用组合？

> 固定流程骨架、少量步骤可覆盖时可以用模板方法继承，例如不同移动操作共享校验和保存流程。治具、质检、物料等横向规则更适合组合成处理器或策略列表，否则基类会不断变大，子类也容易受到无关逻辑影响。

### 11. DynamicObject 最大的问题是什么？

> 字段和类型主要在运行时校验，缺少普通 POJO 的编译期安全。我的应对方式是查询时显式列字段、参考元数据、集中字段转换，并尽早映射成 DTO。更进一步可以用 Repository 封装 DynamicObject，让上层只看到强类型对象。

### C. 配置驱动与规则触发

### 12. 为什么要做成配置驱动，而不是 if-else 写死？

> 规则类型和阈值会变化，如果写死，每次调整都要发版。配置驱动让 Service 读取已生效规则并执行统一匹配。代码只维护规则语义，业务人员可以修改阈值。代价是要增加配置校验、版本管理和错误监控。

### 13. 触发流程的核心不变量是什么？

> 同一份累计量不能被重复消费；同一业务周期不能重复生成等价任务；任务生成和累计扣减必须保持一致；只有已生效、配置完整的规则才能参与判断。

### 14. 为什么触发后使用取模？

> 例如阈值是 10，累计值是 23。清零会丢失超出的 13；取模后保留 3，表示已经消费完整周期，余量进入下一周期。是否需要一次补生成两次任务，则要根据业务规则单独定义。

### 15. 当前取模实现有什么潜在问题？

> 如果同一种类型允许多个不同阈值，代码用该类型的第一个方案阈值扣减，可能与实际命中的方案不一致。需要先明确配置是否唯一；若不唯一，应按命中规则分别计算消费量，或选择最小阈值并定义组合语义。

### 16. `hasPendingTask(mcId)` 能完全防重复吗？

> 不能。它只是在业务层先查，两个并发请求仍可能同时查到不存在然后都创建。应该增加数据库唯一约束或幂等键，去重维度至少包含资源、规则和触发周期，并在冲突时返回已有任务。

### 17. 如何解决统计累加的并发丢失？

> 当前“查询对象—内存加一—保存”可能发生 lost update。可使用 `update ... set count=count+? where id=? and version=?` 的条件更新，失败后重试；或者使用数据库原子累加。只有跨库或高竞争场景才考虑分布式锁。

### 18. 任务创建成功但统计扣减失败怎么办？

> 两者必须位于同一事务中，失败整体回滚。如果任务创建涉及外部系统，应使用本地事务保存事件或 Outbox，再异步投递；消费端根据业务幂等键去重。不能简单捕获异常后继续返回成功。

### 19. 大量 `return` 是好设计吗？

> Guard Clause 能减少嵌套，但配置缺失时全部静默返回会降低可观测性。输入不满足可以 debug 记录，关键配置缺失应 warn，任务创建失败应 error，并携带资源 ID、规则 ID 和 traceId。

### 20. 如果规则配置更新时正在执行触发怎么办？

> 一次触发应该读取同一版本的规则快照。可以给规则增加版本和生效时间，任务记录命中的规则版本；更新配置采用新版本生效，而不是原地修改历史语义。

### D. 查询、聚合与性能

### 21. WIP 聚合为什么用 HashMap？

> 聚合维度是节点 ID，需要频繁按 ID 查找并累加。HashMap 平均查询 O(1)，遍历 n 条业务记录时整体接近 O(n)。`computeIfAbsent` 可以在首次出现节点时创建聚合对象。

### 22. 这个实现如何避免 N+1？

> 它没有对列表中的每个节点分别查业务记录和阈值，而是一次加载全部有效记录、一次加载全部节点配置、一次加载全部阈值，然后在内存中建立 Map。查询次数不会随页面行数线性增长。

### 23. 一次加载全部数据一定比 N+1 好吗？

> 不一定。它减少了往返，但数据量很大时会占用大量内存和 GC。应根据规模选择：小规模可以内存聚合；大规模应让数据库 GROUP BY、分页读取，或者维护增量汇总表。

### 24. 如果改成 SQL 聚合，你会如何设计索引？

> 过滤条件主要是有效状态和节点 ID，分组也是节点 ID，因此会评估 `(status, node_id)` 或 `(node_id, status)` 组合索引。具体顺序要看选择性和主要查询模式，并用执行计划验证，不能只凭感觉加索引。

### 25. 为什么只查询必要字段？

> 平台动态实体可能包含关联对象和子集合。字段裁剪能减少数据库传输、实体构造和内存占用。只做统计时加载节点、状态和数量即可；需要修改保存时才加载完整实体。

### 26. `int` 做聚合有什么风险？

> 如果单条数量或累计规模足够大，`int` 可能溢出。聚合量更稳妥地使用 `long`，数据库字段也要匹配；涉及小数或金额则用 BigDecimal，并明确舍入规则。

### 27. 如何给聚合结果加缓存？

> 先确认接口读多写少和可接受延迟。可以用短 TTL 缓存整个结果或按节点缓存；状态变化后主动失效。还要防缓存击穿和旧数据覆盖。若更新很频繁，增量汇总通常比频繁失效缓存更合适。

### 28. 详情页如何避免和汇总口径不一致？

> 将“有效记录条件”和“状态分类规则”提取为同一规则组件，汇总和明细复用。测试中对同一组数据断言：等待明细之和等于 waitingQty，处理中明细之和等于 processingQty。

### E. API 与数据组装

### 29. 为什么主记录和明细使用两阶段查询？

> 第一阶段得到需要返回的主记录范围；第二阶段收集关联键，用一次 IN 查询加载所有明细。相比每个主记录查询一次，能减少数据库往返，并且便于组装稳定 DTO。

### 30. 当前父子组装的时间复杂度是多少？

> 主记录 n 条、明细 m 条时，当前代码对每个主记录遍历所有明细，最坏接近 O(n×m)，其中 CSV 解析还会重复执行。可以先把明细按关联编号构建 `Map<String, List<Detail>>`，主记录再 O(1) 获取候选明细，使整体接近 O(n+m)。

### 31. 为什么使用 LinkedHashMap？

> 查询按完成时间倒序，LinkedHashMap 在按 ID 去重和查找的同时保留插入顺序，最终转换成 List 后仍保持数据库查询顺序。普通 HashMap 不保证输出顺序。

### 32. 固定 LIMIT 100 有什么问题？

> 调用方无法翻页，`total` 也只是返回数量，不是真实总数。应支持 pageSize 上限、稳定排序和 pageNo，深分页可以使用“时间 + ID”游标。返回结构明确 total 是总记录数还是当前页数量。

### 33. IN 查询有哪些边界？

> 空集合要直接跳过；集合过大可能超过数据库参数限制，应分批或使用关联查询；字段需要索引；输入要去重。还要注意 IN 只解决查询往返，不自动解决返回数据量过大。

### 34. API 为什么要返回 DTO？

> DTO 隔离平台实体，防止内部字段或关联对象泄漏，稳定前后端契约，也便于字段命名、空值和日期格式统一。平台实体结构变化时只修改映射层，不影响接口使用方。

### 35. 当前异常处理有什么安全问题？

> 系统异常直接把 `ex.getMessage()` 返回给调用方，可能暴露表名、字段或内部实现。更合适的是记录完整异常和 traceId，返回统一错误码及安全提示；业务异常才返回具体可理解的原因。

### 36. 日期为什么不建议在 Service 中格式化为字符串？

> Service 返回时间对象或 Instant 更通用，序列化层统一使用 ISO-8601 并明确时区。固定字符串会把展示格式耦合进业务层，也容易造成时区歧义。

### F. 状态、事务与异常

### 37. 为什么后台必须重新查询状态？

> 页面打开后数据可能已经被别人修改，也可能被请求伪造。后台要重新查询当前实体，并验证状态和关联对象。但“重新查询后再保存”仍不能彻底解决并发，还要配合版本号或条件更新。

### 38. 如何设计状态机？

> 先列出状态和允许事件，例如待处理只能进入处理中，处理中才能完成或取消；为每个事件定义前置条件、目标状态和副作用。代码中由统一 StateMachine 校验转换，而不是多个插件散落字符串判断。

### 39. 乐观锁如何落地？

> 实体增加 version。读取时拿到旧 version，更新条件包含 `id、旧状态、旧 version`，成功后 version 加一。受影响行数为 0 表示数据已变化，重新查询并提示用户。适合冲突不频繁、读多写少的场景。

### 40. 幂等和乐观锁有什么区别？

> 乐观锁防止并发覆盖，幂等防止同一请求重复执行。重复请求即使没有并发，也可能生成两条日志；因此关键操作要有 requestId 或业务唯一键。两者解决的问题不同，经常需要同时使用。

### 41. 业务异常和系统异常怎么区分？

> 业务异常是可预期的规则不满足，例如状态不允许，返回明确错误码和提示；系统异常是数据库、空指针或依赖失败，记录堆栈和上下文，对外返回统一提示。不能把所有 Exception 都包装成业务异常。

### 42. 操作日志应该记录什么？

> 记录业务对象 ID、操作类型、操作人、时间、关键状态前后值和 traceId。日志不能包含密码、Token 等敏感信息。审计日志最好与业务事务一致，普通诊断日志则使用结构化字段方便检索。

### G. 代码质量与测试

### 43. 这些 Service 为什么难做单元测试？

> 它们直接调用静态平台 Helper，并在方法内部 new 依赖，无法方便替换数据源和任务服务。可以抽出 Repository 接口、构造注入依赖，并把阈值匹配和聚合提取为纯函数。

### 44. 规则触发应该测试哪些场景？

> 未达到阈值、刚好达到、超过一个周期、超过多个周期、阈值为零、配置缺失、已有未完成任务、多个规则同时命中、任务创建失败、统计保存失败，以及两个请求并发触发。

### 45. WIP 聚合应该测试哪些场景？

> 空数据、关联节点为空、不同状态分类、不同统计单位、阈值缺失、数量为零或负数、超大数量，以及汇总数量与详情数量一致。聚合函数适合做参数化测试。

### 46. 查询 API 应该测试哪些场景？

> 无主记录、主记录无明细、多个主记录共享关联编号、CSV 中有空格和重复值、明细范围为空、100 条边界、IN 集合过大、数据库异常、敏感异常信息不外泄，以及返回顺序稳定。

### 47. 如果让你做 Code Review，你会先指出什么？

> 第一是并发：统计的读改写和任务去重不是原子的。第二是性能：父子数据组装有双层遍历。第三是接口：固定 100 条不是真分页。第四是可观测性：很多配置缺失直接 return。第五是可测试性：静态 Helper 和内部 new 依赖较多。

### 48. 你从这段实习中最大的技术成长是什么？

> 我开始从业务不变量看代码，而不是只关注某个字段怎么改。现在我会先确认合法状态、事务内必须一起成功的副作用、并发下可能重复执行的位置，再考虑查询次数、DTO 边界和测试方式。

---

## 五、面试官让你现场优化代码时的答题模板

### 1. 优化触发 Service

> 我先把数据库访问抽到 Repository，把规则匹配提取为纯函数；然后使用带 version 的条件更新保护累计值，为任务增加唯一业务键；任务创建和累计扣减放在同一事务。最后补充命中、跳过和失败指标。

### 2. 优化聚合 Service

> 小数据量保留批量加载加 HashMap 聚合；大数据量改为数据库分组聚合。统一汇总和详情过滤规则，计数改用 long。若访问频繁，再根据实时性要求选择短缓存或增量汇总表。

### 3. 优化查询 API

> 增加分页参数和稳定排序；主记录查完后收集关联键，明细按关联键预分组，去掉 O(n×m) 双层扫描；统一 DTO 映射和时间格式；系统异常只返回 traceId，不返回内部 message。

---

## 六、不要这样说

| 不建议 | 建议 |
|---|---|
| 我独立负责整个核心系统 | 我独立完成三个具体需求，并参与核心链路维护 |
| 我保证了高并发安全 | 我识别了并发风险，现有代码仍可通过乐观锁和唯一键加强 |
| 我做了性能优化，提升很多 | 我将逐行查询改为批量加载和 Map 聚合，但当时没有完整压测数据 |
| 这是低代码，所以很简单 | 平台提供运行时和数据 API，复杂业务仍由 Java Service 实现 |
| 我们用了分布式事务 | 核心更新依赖平台事务生命周期；跨系统一致性是进一步设计方向 |
| 代码没有问题，已经上线了 | 功能满足当时需求，我也能指出分页、并发和可测试性改进点 |

---

## 七、面试前重点复习源码

仓库根路径：`code/mes/ewhc-himes-mes/src/main/java/ewhc/himes/mes/`

1. `company/xingan/service/StepWipWarnService.java`
   - `buildWipQtyMap`
   - `buildThresholdMap`
   - `loadDetailLotsByStepId`
   - `accumulateQty`
2. `company/lixin/service/WaferInStockListService.java`
   - `listInstockLots`
   - `splitCsv`
   - `waferBelongsToLot`
3. `company/lixin/webapi/WaferInStockListApi.java`
   - `list`
4. 历史提交 `731d45c2d` 中的：
   - `IPQCTriggerService.tryTriggerOnCheckout`
   - `IPQCTriggerService.matchTrigger`
   - `McCheckoutStatsService.accumulateOnMove`
5. `app/plugin/operate/check/CheckInPlugin.java`
   - `onAddValidators`
   - `endOperationTransaction`
6. `app/common/service/impl/AbstractCheckInService.java`
   - `cando`
   - `done`
   - `checkIn`
7. `app/common/service/impl/AbstractMoveService.java`
   - 通用流程骨架、校验、日志和状态更新顺序

历史文件可通过以下命令查看：

```powershell
git show 731d45c2d:code/mes/ewhc-himes-mes/src/main/java/ewhc/himes/mes/app/common/service/impl/IPQCTriggerService.java
```

---

## 八、最终记忆框架

只记住下面四句话，就能覆盖大多数追问：

1. 我完成了规则触发、聚合预警和父子查询 API 三个可证明的功能。
2. 我用分层、服务端校验和 DTO 控制业务边界。
3. 我用批量加载和 Map 聚合避免 N+1，但也能指出内存聚合和双层遍历的限制。
4. 我能识别初版在并发、幂等、分页、异常信息和可测试性上的不足，并给出可落地的优化方案。
