# 读档：通用企业级岗位 · Java 后端开发工程师
输入模式：JD+简历 | JD 来源：用户截图 | 开始：2026-09-14

## 能力维度框架
- D1 Java 基础、集合、JVM、并发（高）
- D2 MySQL、事务、索引与 SQL 优化（高）
- D3 Redis、缓存、消息队列与一致性（高）
- D4 架构分层、设计模式与方案权衡（高）
- D5 测试、交付与工程规范（高）
- D6 故障定位、维护与个人贡献（高）
- D7 AI Coding 的有效使用与风险控制（中）

## 简历经历清单
- 雅鉴生活志：秒杀、一人一单、Kafka 异步落库、多级缓存、数据一致性、限流（①对口可深挖）
- 益普科技 MES：IPQC 巡检触发、Service/CMS 层扩展、接口联调和缺陷修复（①对口可深挖；测试成果需量化）
- 汇丰科技：跨境支付 API 集成架构分析与方案比较（②需量化）
- JVM、并发、SQL 优化等技能声明（③需验证）
- 存储过程、触发器、AI Coding（③JD 缺口）

## 历次记录
### 第 1 次 — 2026-09-14 — 练习模式/深挖技术
- 完成：10/18 题（用户主动结束）
- Q1 MES IPQC 巡检扩展：🟡合格；自评 1/5。能解释模板方法及方案边界，但测试覆盖、交付结果和量化证据不足。
- Q2 Redis+Lua 秒杀及 Kafka 异步落库：🔴弱；自评 1/5。能说明 String/Set 与 Lua 原子性，但不清楚脚本返回协议、订单 ID 生成时点、消息失败、重复消费和 Redis/MySQL 一致性。
- Q3 MES 并发状态流转：🔴弱；自评 1/5。暂不掌握数据库条件更新、乐观锁、悲观锁、多实例锁边界和冲突处理。
- Q4 HashMap 与 ConcurrentHashMap：🟡合格；自评 1/5。掌握结构、冲突处理、树化阈值、负载因子和并发集合选择，但不熟悉索引计算、扩容阈值表述、Java 8 ConcurrentHashMap 同步机制及 null 限制。
- Q5 ThreadPoolExecutor：🔴弱；自评 1/5。仅模糊知道队列满后可能创建线程，尚不掌握核心参数、正确提交顺序、拒绝策略及无界队列风险。
- Q6 MySQL 联合索引与 EXPLAIN：🔴弱；自评 2/5。知道跳过中间列会影响后续索引定位，但误认为查询必须包含全部联合索引列，未掌握前缀可用规则和 EXPLAIN 关键字段。
- Q7 InnoDB 隔离级别、MVCC 与锁：🟡合格；自评 2/5。能定义三类并发现象，掌握 RC/RR 的 Read View 创建差异和间隙锁作用；不熟悉一致性读与当前读的实现区别，以及记录锁、间隙锁、Next-Key Lock 的完整关系。
- Q8 Spring @Transactional 失效场景：🔴弱；自评 1/5。暂不掌握代理调用边界、异常回滚规则、方法可见性与底层事务支持要求。
- Q9 JVM 运行时数据区、可达性分析与 OOM：🟡合格；自评 1/5。能正确区分线程私有/共享区域、对象与局部变量的常规位置，并知道可达性分析；不熟悉栈帧结构、GC Roots 和 OOM 类型。
- Q10 Caffeine+Redis 多级缓存：🟡合格；自评 2/5。能说明 Caffeine→Redis→MySQL 查询链、先更新数据库再删缓存、穿透/击穿/雪崩及常见措施；项目为单实例，但无法说明缓存失效实现，也无法自证简历所写的 Kafka 删除重试与 TTL 兜底。
- 最终维度快照：D1🔴 D2🔴 D3🔴 D4🟡 D5🔴 D6🟡 D7未评估。
- 最弱维度：Java 并发与 Spring 事务、MySQL 索引和并发控制、Redis/Kafka 端到端一致性。
- 信心盲区：无明显高估；整体存在低估，但基础知识确有系统性缺口。
- 待补经历：核实 Kafka 缓存删除重试；补齐 MES 测试、交付与结果证据；明确秒杀订单 ID、消息体和幂等实现。
- 信心盲区：暂未发现；两题均为低自评。
- 待补经历：秒杀链路的真实代码细节与异常路径；MES 自测边界和交付证据。

## 累计诊断
- 优点：能够诚实区分实际实现和改进设想；具备基本的模板方法判断。
- 当前短板：端到端链路描述、消息可靠性、幂等与一致性、数据库并发控制、联合索引与执行计划、Spring 事务边界、Java 集合底层细节、线程池、测试覆盖；需核实简历中的 Kafka 缓存删除重试表述。
- 本次结论：当前不宜把“熟练 JVM/并发/SQL 优化”作为强项展开；应先补底层原理，再把每项技术绑定到真实代码、异常路径和验证结果。

## 具体加强清单

### P0：面试前必须补齐

#### 1. ThreadPoolExecutor
需要掌握：
- 七个核心参数及各自作用。
- 任务提交流程：先核心线程，再队列，队列满后扩到最大线程数，最后执行拒绝策略。
- 四种内置拒绝策略及适用场景。
- 有界队列与无界队列的区别；无界队列为何会让 maximumPoolSize 基本失效。
- CPU 密集型与 I/O 密集型任务的线程数设置思路。
- 线程池需要监控的指标：活跃线程数、队列长度、完成任务数、拒绝次数。

达标标准：能够不看资料画出任务提交流程，并根据一个真实业务场景设计线程池参数，说明队列满时如何降级。

#### 2. Spring 事务
需要掌握：
- @Transactional 基于 Spring AOP 代理生效的原理。
- 同类内部调用、private 方法、非 Spring Bean 调用导致事务失效的原因。
- RuntimeException、Checked Exception 和 rollbackFor 的回滚规则。
- 异常被 catch 后不继续抛出，为什么可能导致事务提交。
- REQUIRED、REQUIRES_NEW 等常用传播行为。
- 事务边界不应包含耗时远程调用的原因。

达标标准：能以“物料状态更新 + 生产记录写入”为例，设计一个正确事务，并指出至少五种事务失效场景。

#### 3. MySQL 并发控制与锁
需要掌握：
- 使用状态条件或 version 字段实现乐观锁。
- UPDATE 后检查 affected rows 的意义。
- SELECT ... FOR UPDATE 的使用条件和风险。
- Record Lock、Gap Lock、Next-Key Lock 的区别。
- synchronized 为什么不能解决多实例并发。
- 唯一索引在防止重复订单、重复任务中的兜底作用。
- 常见死锁原因、排查方式和固定更新顺序等规避方法。

达标标准：能够独立写出防止两个请求同时完成 MES 状态流转的 SQL、事务伪代码和冲突返回逻辑。

#### 4. MySQL 索引与 EXPLAIN
需要掌握：
- InnoDB B+Tree、聚簇索引和二级索引。
- 联合索引的最左前缀原则；跳过中间列时哪些列还能使用。
- 回表、覆盖索引、索引下推。
- 常见索引失效或效果变差的情况：函数、隐式类型转换、前导模糊匹配、低选择性条件等。
- EXPLAIN 的 type、possible_keys、key、key_len、rows、filtered 和 Extra。
- Using filesort、Using temporary 的含义。

达标标准：给定五条 SQL，能够判断索引使用范围，并根据 EXPLAIN 提出有依据的优化方案。

#### 5. Redis + Kafka + MySQL 一致性
需要掌握：
- 秒杀库存 Key、用户去重 Set 以及 Lua 脚本的输入、执行步骤和返回码。
- 订单 ID 应在什么阶段生成；Kafka 消息至少包含哪些字段。
- Lua 原子性只覆盖 Redis 内部，不能自动保证 Redis、Kafka、MySQL 一致。
- Kafka 生产端确认、失败重试和发送结果处理。
- 消费端使用订单 ID 或“用户 ID + 优惠券 ID”唯一约束实现幂等。
- 数据库事务中完成订单写入和数据库库存更新。
- 消息重复、消息丢失、消费者宕机、消息积压的处理方式。
- 可靠消息、事务消息表、死信队列和定时对账补偿的基本思路。

达标标准：能够画出“Redis 预扣 → Kafka → MySQL 落库”的完整时序，并逐个说明每个节点失败后的结果和补偿方式。

### P1：补强简历中已经声明的能力

#### 6. HashMap 与 ConcurrentHashMap
需要掌握：
- Java 8 HashMap 的数组、链表和红黑树结构。
- hash 扰动与 `(n - 1) & hash` 下标计算。
- 扩容阈值、树化阈值及容量保持 2 的幂的原因。
- 并发写 HashMap 的风险。
- Java 8 ConcurrentHashMap 的 CAS + 桶级 synchronized 思路。
- ConcurrentHashMap 禁止 null Key/Value 的原因。

达标标准：在三分钟内完整讲清 put 流程、扩容和并发替代方案。

#### 7. JVM 与垃圾回收
需要掌握：
- 程序计数器、虚拟机栈、本地方法栈、堆、方法区/元空间。
- 栈帧中的局部变量表、操作数栈、动态链接和返回地址。
- 可达性分析及常见 GC Roots。
- 新生代/老年代、Minor GC、Major/Full GC 的基本区别。
- 常见垃圾回收算法和至少一种常用收集器的工作思路。
- Java heap space、Metaspace、Direct buffer memory、Unable to create native thread 和 StackOverflowError。
- 常用排查工具：jps、jstack、jmap、jstat，以及堆转储分析的基本流程。

达标标准：能根据“接口变慢、频繁 Full GC、内存持续上涨”等现象给出一套初步排查步骤。

#### 8. 多级缓存
需要掌握：
- Caffeine → Redis → MySQL 的查询和回填流程。
- 数据更新后删除 Redis 与本地缓存的具体顺序。
- 单实例与多实例下，本地缓存失效机制的区别。
- 缓存穿透、击穿、雪崩及各自解决方案。
- 缓存空值、互斥锁、逻辑过期、随机 TTL 的适用边界。
- 删除缓存失败时的重试、消息通知和 TTL 兜底。

达标标准：能说明一次查询和一次更新的完整缓存链路，并解释出现短暂不一致时系统如何恢复。

#### 9. 测试与交付
需要掌握：
- 单元测试、集成测试、接口测试、回归测试和并发测试的职责区别。
- 正常值、阈值前、等于阈值、超过阈值、空值、非法状态、重复请求和并发请求。
- 使用 JUnit、Mockito、SpringBootTest 等工具的基本方法。
- 如何验证数据库状态、巡检任务、生产记录和日志同时符合预期。
- 如何记录缺陷、定位根因、提交修复并完成回归。

达标标准：为 IPQC 巡检触发功能写出至少 10 个具体测试用例，其中必须包含边界、异常、重复和并发场景。

### P2：针对 JD 补充

#### 10. 存储过程、触发器与主流数据库
需要掌握：
- 存储过程与普通 SQL/应用层逻辑的区别和适用场景。
- IN、OUT、INOUT 参数和基本异常处理。
- BEFORE/AFTER、INSERT/UPDATE/DELETE 触发器。
- 触发器隐藏副作用、调试困难和事务边界风险。
- MySQL 与 Oracle 在分页、序列/自增键、常用函数等方面的基础差异。

达标标准：能够写一个简单存储过程和触发器，并说明为什么实际项目中不能滥用。

#### 11. AI Coding
需要掌握：
- 用 AI 生成样板代码、单元测试、SQL 草稿和接口文档的合理场景。
- 如何提供上下文、约束输出并拆分任务。
- 必须人工检查的内容：业务正确性、并发安全、事务、权限、敏感信息、依赖版本和异常处理。
- 通过编译、静态检查、单元测试、集成测试和代码评审验证生成代码。
- 不向外部模型提交客户代码、密钥和生产数据。

达标标准：准备一个真实例子，讲清“任务是什么、AI 生成了什么、你发现并修正了什么、最终如何验证”。

## 简历项目核查清单

### 雅鉴生活志
- 回看代码，确认订单 ID 在 Lua 前还是 Lua 后生成。
- 写出 Lua 脚本真实的 Key、参数、返回码和执行顺序。
- 确认 Kafka 消息的真实字段、生产者配置和消费逻辑。
- 确认是否真的实现发送重试、消费幂等、缓存删除重试和 TTL 兜底。
- 如果没有实现，不要在面试中说成已有功能；必要时降低简历表述强度。
- 准备至少一个并发量、库存量或测试结果等真实数字。

### MES IPQC
- 记住真实父类、子类、关键方法和配置字段名称。
- 画出下机/移站后触发巡检的调用链。
- 准备按批数、按颗数、阈值边界和非法状态的真实测试过程。
- 明确哪些工作由自己完成，哪些由联调或测试人员完成。
- 补充一个真实缺陷：现象、日志或数据、定位过程、修复和验证结果。

### 汇丰架构分析
- 明确两种技术路线的具体名称。
- 从功能适配、成本、周期、协作和维护性逐项说明差异。
- 说明你产出的文档、评审参与者和最终是否被采纳。
- 如果未参与最终决策，应明确表述为“提供分析输入”，不要说成主导选型。

## 建议复习顺序
1. ThreadPoolExecutor + Spring 事务。
2. MySQL 索引、MVCC、锁和乐观并发控制。
3. Redis Lua、Kafka 可靠消息、消费幂等和数据一致性。
4. 回看两个项目源码，补齐真实调用链和异常路径。
5. JVM、HashMap、ConcurrentHashMap。
6. 测试设计、存储过程/触发器和 AI Coding。
